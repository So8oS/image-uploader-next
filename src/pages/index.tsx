import { type NextPage } from "next";

import Box from "../components/box";

const Home: NextPage = () => {
  return (
    <div className="h-screen w-full bg-[#FAFAFB] flex flex-col items-center justify-center ">
      <Box />
    </div>
  );
};

export default Home;
